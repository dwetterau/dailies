# Dailies - Agent Context

The Expo / React Native app moved to **Tasky** at `tasky/mobile/`.

The `convex/` directory is the old Dailies backend. Leave it as-is. Do not add
new reads or auth against it. Do not delete it unless asked.
